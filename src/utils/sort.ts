import { SortBy } from '../App';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const sort = (array: any[], sortBy: SortBy = 'ASC', objectKey: string | undefined) => {
  if (!objectKey) {}
};

export default sort;
